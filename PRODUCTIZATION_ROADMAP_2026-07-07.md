# TripPlan / AgentApp / Kids Games 产品化路线

更新日期：2026-07-07

## 目标

这三个项目后续不再按 demo 思路扩展，而按产品线推进：

- `tripplan`：状态驱动旅行规划 Agent，目标是“可持续迭代的旅行规划工具”。
- `agentapp`：移动端多 Agent 助手，目标是“个人 AI 助手入口”。
- `kids-games`：儿童教育小游戏合集，目标是“可运营的儿童启蒙内容产品”。

共同产品化标准：

1. 每个功能必须有清晰目标用户和核心场景。
2. 关键状态必须结构化、可恢复、可测试。
3. AI 输出不能直接污染核心状态，必须通过工具、schema 或 parser。
4. 离线/无 key 模式必须可演示。
5. 文档、元信息、测试和实际内容规模保持一致。

## 1. TripPlan：状态驱动旅行规划 Agent

### 当前优势

- 行程是 `Itinerary` 可变状态，Agent 通过工具修改状态，不直接吐 JSON。
- `retimeline()` 保证时间链自洽，核心逻辑可单测。
- 高德 API + 种子数据双模式，离线可跑。
- 有 SSE 实时事件、服务端会话存档和 CLI 调试入口。
- 已落地基础约束评估：强度、每日点位、交通时长、最晚结束、预算、开放时间、亲子/老人/雨天/餐饮偏好。
- 已落地多方案评分：可按轻松/均衡/紧凑三种视角给当前行程打分并解释扣分原因。
- 已落地约束修复建议：从冲突检测进一步给出可执行调整动作和可替换 POI 候选。
- 已落地 Markdown 导出：可把结构化行程导出为包含时间表、交通、费用、备注的清单，并已接入前端下载入口。

### 产品定位

“会持续改计划的旅行规划助手”：用户不是一次性拿一份攻略，而是在对话中不断调整城市、日期、预算、偏好、交通和景点顺序，行程始终保持时间、路线和地图自洽。

### 下一阶段功能方向

- 约束系统：预算、每日强度、开放时间、餐饮偏好、亲子/老人/雨天、替代建议已完成第一步，后续补更多城市/季节规则。
- 多方案：轻松版、效率版、亲子版、人文版，并支持方案对比。
- 可信解释：每次调整说明“为什么这样排”“受哪些约束影响”。
- 导出能力：Markdown 日程、费用清单和前端下载入口已完成第一步，后续补地图路线和 PDF。
- 城市包：北京、上海、杭州、成都等离线种子城市，提高无 key 演示质量。

### 专业化验收

- 一条复杂需求能生成多日行程，并支持 3 次以上连续修改。
- 每次增删改后 `Itinerary.revision` 增加，时间链不倒挂。
- 无 AMap/LLM key 时仍能跑完整 demo。
- 核心行程变更都有单测覆盖。

## 2. AgentApp：移动端多 Agent 助手

### 当前优势

- Flutter Android 优先，已有 Riverpod、SQLite、安全存储、Markdown、流式对话。
- 有工作/学习/生活/通用四类 Agent 和 RouterAgent。
- 长期记忆已落在本地仓储，隐私卖点明确。
- 已落地路由透明度：路由 Agent 返回目标 Agent 与中文理由，助手消息展示并持久化路由原因。
- 已落地记忆管理页：用户可查看、编辑、删除本地长期记忆。
- 已落地场景模板入口：会议纪要、邮件润色、学习计划、概念讲解、旅行清单、购物比较。
- 已落地隐私状态与记忆开关：设置页展示密钥状态、本地数据说明，并可关闭长期记忆增强。
- 已落地本地数据可控：设置页可复制 Markdown 数据导出，也可清除本机会话和长期记忆。
- 已落地本地提醒第一步：初始化通知服务、Android 通知权限、设置页测试通知、提醒文本解析、对话页一键填入提醒草稿、提醒记录持久化/取消和“提醒我”场景模板。

### 产品定位

“本地优先的个人 AI 助手”：不是又一个聊天壳，而是能记住用户偏好、按场景路由、逐步接入手机能力的移动端助手。

### 下一阶段功能方向

- 记忆管理：用户可查看、编辑、删除长期记忆。
- 路由透明：展示本次被分配到哪个 Agent，以及为何分配。
- 手机原生能力：剪贴板、通知测试、基础定时提醒、提醒草稿编辑、提醒记录管理已完成第一步，后续补分享入口和语音输入。
- 场景模板：会议纪要、学习计划、旅行清单、购物比较、提醒计划已完成第一步；提醒文本可解析进本地草稿，对话页可一键带入，后续补日历落地。
- 隐私模式：本地数据说明、密钥状态、无记忆模式、数据导出/清除已完成第一步，后续补更细的选择性导出和密钥清除。

### 专业化验收

- Android 端能稳定完成设置 key、发起流式对话、保存历史、恢复会话。
- 路由失败时自动回退通用 Agent，不影响主流程。
- 记忆提取失败不影响对话，且用户能删除误记忆。
- 核心服务有 Flutter test 覆盖。

## 3. Kids Games：儿童教育小游戏合集

### 当前优势

- 已有 81 个游戏注册在 `src/games/registry.ts`。
- 零第三方游戏引擎，Vite + TypeScript + Canvas/DOM/SVG，适合静态部署。
- 有存档、星级、成就、家长设置、自适应难度等基础系统。
- 已落地家长报告：按能力域汇总体验、通关、累计时长、平均单局时长、优势能力、建议练习与推荐游戏。
- 已落地 PWA 基础能力：manifest、图标、service worker 离线应用壳缓存。
- 已落地内容发现第一步：大厅支持按能力域、年龄、预计时长、通关状态和搜索组合筛选，筛选逻辑有独立测试。

### 产品定位

“3-6 岁儿童启蒙游戏屋”：不是单个小游戏，而是按年龄、能力域和难度组织的长期内容产品。

### 下一阶段功能方向

- 内容运营：按能力域、年龄、预计时长和通关状态筛选已完成第一步，后续补难度标签、主题活动和推荐位。
- 家长报告：游玩次数、通关数、累计时长、平均单局时长、优势能力、建议练习已完成第一步，后续补趋势图。
- 内容质量标准：每个游戏必须有教育内核、反馈机制、失败保护、结算标准。
- PWA 离线安装：manifest 与 service worker 已完成第一步，后续补更细粒度缓存策略。
- 版本化内容：新增游戏必须更新注册表并通过完整性测试。

### 专业化验收

- `GAMES` 是内容规模唯一事实来源，README/package/测试不得漂移。
- 每个游戏都有 title、subtitle、icon、theme、age、tag。
- 存档能覆盖全部游戏。
- build 后可作为静态站点部署。

## 4. 三项目协同

可共享的产品能力：

- 状态与存档：TripPlan 会话、AgentApp 历史/记忆、Kids Games 进度都应有版本号和迁移策略。
- 离线优先：三个项目都必须保留无 key/无后端演示路径。
- 解释性：TripPlan 解释行程调整，AgentApp 解释路由，Kids Games 向家长解释能力训练。
- 质量门禁：新增内容必须带元信息和测试。

不建议过早共享代码。三个项目技术栈不同，当前更重要的是共享产品方法和验收标准，而不是抽公共包。

## 5. 近期优先级

1. `kids-games`：注册表完整性、文档一致性和大厅内容发现已完成第一步，下一步补难度标签、推荐位和家长趋势图。
2. `tripplan`：约束层、多方案评分、餐饮偏好、亲子/老人/雨天约束、自动替代建议、Markdown 导出与前端下载入口已完成第一步，下一步补 PDF。
3. `agentapp`：路由透明度、记忆管理、场景模板、隐私模式、数据导出/清除、本地通知、基础定时提醒、提醒文本解析、对话页一键带入提醒草稿和提醒列表管理已完成第一步，下一步补日历落地或语音输入。
