import type { ToolMeta } from '../../../types.ts';

const meta: ToolMeta = {
  id: 'image-crop',
  groupId: 'file',
  title: '图片裁剪',
  summary: '输入裁剪区域坐标裁剪图片，实时预览结果。',
  keywords: ['image', 'crop', 'cut', '图片', '裁剪'],
  icon: '✂️',
};

export default meta;
