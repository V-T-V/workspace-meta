import type { ToolMeta } from '../../../types.ts';

const meta: ToolMeta = {
  id: 'regex-tester',
  groupId: 'text',
  title: '正则表达式测试',
  summary: '实时测试正则匹配，列出全部命中与捕获组，支持标志位。',
  keywords: ['regex', 'regexp', 'pattern', 'match', '正则', '匹配'],
  icon: '🔮',
};

export default meta;
