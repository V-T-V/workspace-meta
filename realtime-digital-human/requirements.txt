# 运行时核心依赖（与 pyproject.toml [project].dependencies 保持一致）。
# 单一数据源原则（M12）：此处只列核心，重依赖按需用 pyproject 的 optional-dependencies 安装：
#   pip install -e ".[asr,tts-edge]"   # ASR + TTS
#   pip install -e ".[dev]"            # 开发工具
#
# 重依赖（faster-whisper / edge-tts / MuseTalk / CosyVoice）不在此文件，
# 避免与 pyproject.toml 双重维护漂移。

fastapi>=0.110
uvicorn[standard]>=0.27
pyyaml>=6.0
aiohttp>=3.9
websockets>=12.0
miniaudio>=1.59        # edge-tts MP3→PCM16 解码（喂给 MuseTalk）
